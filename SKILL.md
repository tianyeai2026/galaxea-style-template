---
name: galaxea-style-template
description: Galaxea（星海图）风格网站模板与设计系统。参考 galaxea-ai.com/cn 的视觉风格，提供完整的深色导航栏、全屏滚动首页、橙色强调色、右侧社交图标栏、黑色多列页脚等组件模板。当用户需要创建或改造企业官网、产品展示页、品牌落地页，且期望类似 galaxea-ai.com 的高端科技感风格时使用；也适用于将现有前端页面迁移为 galaxea 风格（导航、Hero、卡片、页脚、社交栏、响应式汉堡菜单等）。
---

# Galaxea 风格模板

基于 https://galaxea-ai.com/cn 提炼的可复用网站设计系统与前端模板。

## 快速开始

1. 复制 `assets/frontend-template/` 到项目中作为起点
2. 打开 `index.html` 即可预览完整效果（需联网加载 Swiper CDN）
3. 替换 Logo、文案、图片、链接为实际内容
4. 调整 `css/style.css` 顶部 CSS 变量中的品牌色

## 核心设计特征

| 特征 | 规格 |
|------|------|
| 主色调 | 橙色 `#ff5a00`（按钮/CTA）、`#ff6b35`（标题强调） |
| 导航栏 | fixed 64px，透明→滚动后 `#272727`，0.3s 过渡 |
| 首页 | Swiper 垂直全屏滚动，5屏，底部 01/02/03 页码 |
| 字体 | Alibaba PuHuiTi，大标题 60px/700，正文 16px |
| 社交栏 | fixed 右侧垂直居中，白色圆角栏，48px 图标 |
| 页脚 | 黑色 `#000`，5列导航，咨询热线大字号，版权备案 |
| 响应式 | >1100px 水平导航；≤1100px 汉堡全屏菜单；<768px 单列 |

详细设计规范见 [references/design-system.md](references/design-system.md)，包含完整色板、字号、间距、组件、动画、断点。

## 模板文件结构

```
assets/frontend-template/
├── index.html          # 完整页面（导航+5屏滚动+页脚+社交栏）
├── css/style.css       # 全部样式（含 CSS 变量、响应式）
└── js/main.js          # 导航滚动、汉堡菜单、Swiper、进入动画
```

## 组件清单

模板已内置以下可复用组件，直接复制修改即可：

1. **响应式导航栏** — 桌面水平菜单 / 移动端汉堡全屏菜单，滚动透明→深色变色
2. **汉堡菜单横线动画** — 三条线点击后旋转变成 X，0.3s 过渡
3. **全屏滚动首页** — Swiper vertical，5屏，wheel 节流，键盘/触摸支持，底部数字页码
4. **深浅背景交替** — 6套主题色板循环（深/浅交替），按钮橙黄渐变
5. **Hero 区** — 左文右图，渐变标题，胶囊 CTA 按钮，底部鼠标滚动指示器
6. **右侧导航点** — fixed 右侧，当前页橙色放大，可点击跳转
7. **特性卡片** — 3列网格，hover 上浮+阴影加深
8. **技术布局** — 左文右图，带勾选列表
9. **场景网格** — 4列等比卡片，hover 缩放
10. **新闻卡片** — 3列，图片+标题+日期，hover 上浮
11. **右侧社交栏** — 微信/视频号/小红书/领英彩色图标，hover 背景变化（预留二维码弹窗）
12. **黑色页脚** — 5列导航+咨询热线大字号+分割线+版权备案
13. **滚动进入动画** — IntersectionObserver 淡入上移，按顺序延迟

## 改造现有项目的流程

当需要把现有页面改成 galaxea 风格时：

1. **先读设计规范**：Read `references/design-system.md`，确认颜色、字体、间距
2. **导航栏**：高度 64px，背景 `#272727`，白色文字，当前页橙色下划线；小屏用汉堡
3. **首页**：如需全屏滚动，引入 Swiper，参考模板的 vertical 配置
4. **颜色替换**：把品牌色统一为 `#ff5a00`，标题强调用 `#ff6b35`
5. **页脚**：黑色背景，多列布局，参考模板结构
6. **社交栏**：fixed 定位，参考 `.social-sidebar` 样式
7. **响应式**：断点 1100px（导航切换）和 768px（单列布局）

## 注意事项

- 模板使用 Swiper 11 CDN，离线环境需下载到本地
- 字体 Alibaba PuHuiTi 需自行引入或替换为系统字体
- 社交图标点击行为预留为空，按需添加二维码弹窗或跳转
- 页脚的电话、版权、备案号为占位内容，替换为实际信息
